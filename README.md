# 学习协作系统｜软件测试作品集

> 面向软件测试应届生岗位的项目作品集：以一个真实可运行的前后端学习协作系统为测试对象，完成需求梳理、用例设计、手工执行、接口与权限验证、SQL 数据核对、缺陷记录和证据归档。

本仓库同时保留被测系统源码与测试成果。测试结论只来自当前代码和已经实际执行的场景；源码预测、需求待确认项与已确认缺陷分别记录，不把未执行内容计入结果。

## 核心测试成果

| 指标 | 数量 |
| --- | ---: |
| 累计执行 | **124** |
| Pass | **77** |
| Fail | **37** |
| Risk | **10** |
| 已确认缺陷 | **21** |
| 业务规则缺口 | **1** |
| 风险候选 | **9** |
| 累计问题记录 | **31** |

统计口径以管理员模块完成后的公开基线为准。Risk 表示实际观察到、但因需求规则未明确而暂不直接判定为缺陷的结果。

### 最终封版回归（独立统计）

| 范围 | 结果 | 通过率 |
| --- | ---: | ---: |
| REG-FINAL-001～REG-FINAL-015 | **15 / 15 Pass** | **100%** |

这15条用于验证修复后版本的核心流程、关键权限和数据清理，**不并入上面的124条正式功能测试统计**。完整结果见[最终封版回归报告](docs/test-execution-final-regression.md)。

### 缺陷闭环状态

| 口径 | 数量 |
| --- | ---: |
| 累计确认缺陷 | **21** |
| 已修复并验证 | **9** |
| 当前未修复确认缺陷 | **12** |

本统计依据 [`docs/bug-report.md`](docs/bug-report.md) 中全部确认缺陷的当前追加状态重新计算，不包含1个业务规则缺口和9个风险候选。

## 被测系统简介

学习协作系统采用 React、Node.js、Express、MySQL 和 Socket.IO 实现，主要包含：

- 登录、注册与登录状态管理
- 小组创建、邀请、成员关系和权限控制
- 三状态任务看板与协作操作
- 小组讨论、实时消息和 `@` 提及
- 文件上传、下载、删除与资料共享
- 通知、Dashboard、个人资料和密码修改
- 管理员概览、用户管理、小组监督、任务监管和资料审查

## 我的测试职责

- 阅读前端页面、后端接口和数据库初始化代码，梳理真实功能与权限规则。
- 设计正常、异常、边界、角色权限和数据一致性场景。
- 通过页面、浏览器 DevTools、接口请求、SQL 查询和本地文件目录执行验证。
- 保存最小完整证据链，区分 Pass、Fail、Risk 和仅源码预测内容。
- 按根因归并缺陷，维护执行报告、缺陷报告和累计统计。
- 在每轮测试结束后恢复临时账号、成员关系、任务、通知、文件和数据库记录。

## 测试环境与工具

| 类别 | 使用内容 |
| --- | --- |
| 前端 | React 19、Vite、React Router、Axios |
| 后端 | Node.js、Express、Socket.IO、Multer |
| 数据库 | MySQL、mysql2 |
| 测试工具 | Chrome DevTools（Network、Console、Application）、SQL 查询工具、PowerShell |
| 证据类型 | UI、HTTP 请求与响应、Console、SQL 查询、本地物理文件 |

## 测试方法

本项目以适合应届生展示的基础测试方法为主：

- 正常流程、异常流程和必填项检查
- 等价类、边界值和格式校验
- 接口状态码、响应体和数据库结果核对
- 组长、普通成员、非成员、管理员和未登录用户的权限验证
- IDOR、跨组访问、宽松 ID 解析和静态资源直链等安全边界检查
- 数据库记录、页面展示与磁盘文件的三方一致性检查
- 页面刷新、状态持久化和 Socket.IO 实时消息验证

本仓库目前不包含自动化测试框架、性能测试或渗透测试成果。

## 测试覆盖矩阵

| 模块 | 执行 | Pass | Fail | Risk | 主要覆盖 |
| --- | ---: | ---: | ---: | ---: | --- |
| 登录与注册 | 11 | 7 | 4 | 0 | 登录状态、重复数据、邮箱、密码边界 |
| 小组基础管理 | 4 | 3 | 0 | 1 | 创建、必填、重复名称、访问保护 |
| 小组邀请与成员权限 | 8 | 7 | 0 | 1 | 邀请、接受/拒绝、重复邀请、越权接口 |
| 任务管理 | 21 | 10 | 11 | 0 | 状态流转、成员协作、跨组权限、输入与一致性 |
| 讨论与实时消息 | 19 | 9 | 5 | 5 | 历史消息、实时通信、非成员访问、`@` 提及 |
| 文件上传与资料共享 | 24 | 11 | 11 | 2 | 上传、下载、删除、静态直链、文件系统一致性 |
| 通知 | 10 | 8 | 1 | 1 | 邀请状态、已读、删除、对象级权限和 ID 边界 |
| Dashboard | 9 | 9 | 0 | 0 | 小组、任务、资料统计联动与权限隔离 |
| 个人资料与密码 | 10 | 7 | 2 | 1 | 资料同步、越权、邮箱、改密和旧会话 |
| 管理员 | 8 | 5 | 2 | 1 | 登录路由、统计、CRUD、权限隔离和关联清理 |
| **累计** | **124** | **77** | **37** | **10** | — |

## 代表性缺陷闭环

以下案例均保留修复前证据、代码修复、定向验证、影响回归和修复后证据，详细历史见[缺陷与风险报告](docs/bug-report.md)。

| 缺陷 | 问题与风险 | 修复 | 验证结果 | 状态 |
| --- | --- | --- | --- | --- |
| `BUG-FILE-001 / 002` 文件权限与身份伪造 | 匿名或非成员可操作文件，上传者身份可由客户端伪造 | REST接口增加认证、成员与删除授权；上传者只取认证用户 | 合法成员流程正常，匿名/非成员401或403且无DB/磁盘副作用 | 已修复并验证 |
| `BUG-TASK-001` 任务跨组越权 | 非成员可读取、创建、修改和删除其他小组任务 | 按目标group或task真实归属执行成员授权 | 非成员读写403；成员任务生命周期正常 | 已修复并验证 |
| `BUG-DISC-001` 讨论越权与自动入组 | 非成员可读取、发送讨论，发送还会绕过邀请自动入组 | GET/POST增加成员授权并移除发送路径自动入组 | 非成员读取/发送403，成员关系和discussion无副作用 | 已修复并验证 |
| `BUG-ADMIN-002` 物理文件生命周期 | 管理员解散小组后数据库已清理但物理文件残留 | 删除前读取明确文件清单，数据库成功后逐个安全清理 | 有文件、无文件和缺失文件场景通过，其他小组文件未受影响 | 已修复并验证 |
| `BUG-FILE-007` 文件获取链接 | 资料集锦链接落到前端5173并返回HTML | 使用统一后端资源地址解析文件URL | TXT响应200，类型和内容正确 | 已修复并验证 |
| `BUG-LR-002 / 003` 后端输入校验 | 过短密码和非法邮箱可绕过前端写入 | 所有真实写入口统一执行密码最小长度和邮箱格式校验 | 非法输入400且无落库，合法边界和登录流程正常 | 已修复并验证 |
| `BUG-ADMIN-001` 管理员用户ID宽松解析 | `29abc`会命中真实ID 29 | 路径参数必须完整匹配安全正整数 | 非法ID统一400且无副作用，合法CRUD保持正常 | 已修复并验证 |

## 精选测试证据

| 最终非成员任务、讨论、文件权限 | 最终文件获取生命周期 |
| --- | --- |
| ![非成员受保护资源403](docs/screenshots/REG-FINAL-006-nonmember-protected-403.png) | ![真实文件获取](docs/screenshots/REG-FINAL-010-file-lifecycle.png) |

| 讨论越权被拒绝 | 最终group31基线 |
| --- | --- |
| ![非成员讨论403](docs/screenshots/REG-FINAL-009-discussion-nonmember-403.png) | ![group31最终基线](docs/screenshots/REG-FINAL-015-group31-baseline.png) |

纳入本轮13张最终回归截图后，完整的 **374张PNG正式证据**按用例编号保存在 [`docs/screenshots/`](docs/screenshots/)。首页只展示最能说明权限、文件生命周期和最终基线的证据；修复前证据继续保留在缺陷报告中。

## 核心文档导航

- [完整测试文档索引](docs/README.md)
- [系统测试分析与覆盖说明](docs/test-analysis.md)
- [缺陷与风险报告](docs/bug-report.md)
- [最终封版回归报告](docs/test-execution-final-regression.md)
- [登录注册测试用例](docs/test-cases-login-register.md)
- [登录注册执行报告](docs/test-execution-login-register.md)
- [小组成员与权限执行报告](docs/test-execution-group-members.md)
- [任务管理执行报告](docs/test-execution-tasks.md)
- [讨论与实时消息执行报告](docs/test-execution-discussions.md)
- [文件与资料共享执行报告](docs/test-execution-files.md)
- [管理员模块执行报告](docs/test-execution-admin.md)

## 项目运行方式

### 1. 环境要求

- Node.js 20.19+ 或 22.12+
- MySQL 8.x
- npm

### 2. 安装依赖

```bash
npm install
```

### 3. 配置环境变量

将 `.env.example` 复制为 `.env`，填写本机数据库连接信息，并为 `AUTH_TOKEN_SECRET` 设置随机长字符串。不要提交 `.env`。

```dotenv
DB_HOST=localhost
DB_USER=<your-user>
DB_PASSWORD=<your-password>
DB_NAME=my_study_system
AUTH_TOKEN_SECRET=<a-long-random-secret>
```

先在 MySQL 中创建与 `DB_NAME` 对应的空数据库。当前实际运行结构由后端启动时的 `createTables()` 自动创建和兼容升级。

> 注意：`server/create-tables.sql` 是早期基础脚本，只包含部分旧表结构，表名、任务状态和字段与当前运行时结构并不完全一致，不能作为当前版本的完整初始化脚本。当前版本应以 `server/index.js` 中的运行时建表逻辑为准。

### 4. 启动后端

```bash
node server/index.js
```

后端默认监听 `http://localhost:3001`。

### 5. 启动前端

在另一个终端运行：

```bash
npm run dev
```

前端开发地址通常为 `http://localhost:5173`。服务启动时可能创建本地演示账号，这些账号只用于本地展示，不应直接用于公网部署或生产环境。

## Known Issues

项目仍透明保留12个未修复确认缺陷，重点包括：

- `BUG-DISC-002`（高 / P0）：Socket.IO连接和房间加入缺少认证及成员授权；REST讨论权限已修复不代表实时消息权限完全安全。
- `BUG-FILE-003`（高 / P0）：`/uploads`静态资源仍可匿名直接访问。
- `BUG-FILE-004`（高 / P0）：文件类型与MIME安全校验仍缺失。
- 其余未修复确认缺陷为 `BUG-LR-001`、`BUG-PROF-001`、`BUG-TASK-002～004`、`BUG-DISC-003`、`BUG-FILE-005～006`、`BUG-NOTIF-001`，涉及错误语义、输入边界和低频异常路径。

完整复现证据、严重程度和优先级见[缺陷与风险报告](docs/bug-report.md)。当前项目定位为**软件测试求职作品集 / 本地演示项目**，不描述为公网生产安全版本。另有以下已知限制：

- `server/create-tables.sql` 不是当前完整数据库结构，运行时建表逻辑才是当前基线。
- Dashboard 使用页面加载时快照，不通过轮询或Socket自动刷新。
- 固定演示账号和初始化数据仅用于本地展示。
- 当前成果以手工功能、接口、权限和数据一致性测试为主，未虚构自动化、性能或渗透测试经历。

## 仓库结构

```text
.
├─ src/                    前端页面、组件与 API 客户端
├─ server/                 后端接口、运行时建表逻辑与上传服务
├─ docs/                   测试分析、测试用例、执行报告和缺陷报告
│  ├─ README.md            完整测试文档索引
│  └─ screenshots/         按测试编号保存的正式证据
├─ .env.example            环境变量示例
├─ package.json            依赖与前端运行脚本
└─ README.md               作品集首页
```

## 当前作品集状态

- 业务代码基线与测试对象一致。
- 主要业务模块和管理员模块均已有正式执行记录。
- 测试数据已按各模块报告中的说明完成清理或恢复。
- 正式功能测试保持124条：Pass 77、Fail 37、Risk 10。
- 修复后最终封版回归15/15 Pass，临时数据和uploads均已恢复基线。
- 21个确认缺陷中9个已修复并验证，12个作为Known Issues透明保留。
